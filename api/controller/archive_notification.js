const Controller = require('../controller/controller');
const { ArchiveNotification } = require('../model');

class ArchiveNotificationController extends Controller {
}

module.exports = new ArchiveNotificationController(ArchiveNotification);
